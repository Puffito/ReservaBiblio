let editReservation = document.getElementById("editarReserva");

if (editReservation != null) {
    editReservation.addEventListener("click", (e) => {
        location.href = "/editarReserva"
    });
}